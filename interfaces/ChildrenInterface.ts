export interface TextInterface {
    text: string;
    bold?: Boolean;
    italic?: Boolean;
    underline?: Boolean;
}

export interface ImageInterface {
    title: string;
    height: string;
    width: string;
    src: string;
}